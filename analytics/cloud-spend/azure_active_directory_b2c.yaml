integrationType:
- azure_subscription
name: Azure Active Directory B2C
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Azure Active Directory B2C' group by 1,2;
status: active
tables:
- Azure Active Directory B2C
tags:
  category:
  - IAM
