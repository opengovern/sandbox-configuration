connectors:
- Azure
name: Azure Load Testing
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Azure Load Testing' group by 1,2;
status: active
tables:
- Azure Load Testing
tags:
  category:
  - DevOps
