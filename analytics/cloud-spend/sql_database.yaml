integrationType:
- azure_subscription
name: SQL Database
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'SQL Database' group by 1,2;
status: active
tables:
- SQL Database
tags:
  category:
  - Database
