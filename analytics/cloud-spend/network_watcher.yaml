integrationType:
- azure_subscription
name: Network Watcher
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Network Watcher' group by 1,2;
status: active
tables:
- Network Watcher
tags:
  category:
  - Monitoring
