integrationType:
- azure_subscription
name: Cloud Services
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Cloud Services' group by 1,2;
status: active
tables:
- Cloud Services
tags:
  category:
  - Legacy PaaS
