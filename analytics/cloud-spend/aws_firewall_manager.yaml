connectors:
- AWS
name: AWS Firewall Manager
query: SELECT og_account_id, period_start::date::text as date, sum(amortized_cost_amount) FROM aws_cost_by_service_daily WHERE service = 'AWS Firewall Manager' group by 1,2;
status: active
tables:
- AWS Firewall Manager
tags:
  category:
  - Security
