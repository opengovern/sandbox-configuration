connectors:
- Azure
name: Azure Cosmos DB
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Azure Cosmos DB' group by 1,2;
status: active
tables:
- Azure Cosmos DB
tags:
  category:
  - Database
