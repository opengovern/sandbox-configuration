integrationType:
- azure_subscription
name: Azure Spring Cloud
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Azure Spring Cloud' group by 1,2;
status: active
tables:
- Azure Spring Cloud
tags:
  category:
  - PaaS
