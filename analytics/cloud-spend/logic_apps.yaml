integrationType:
- azure_subscription
name: Logic Apps
query: SELECT og_account_id, usage_date::text as date, sum(cost) FROM azure_costmanagement_costbyresourcetype WHERE service_name = 'Logic Apps' group by 1,2;
status: active
tables:
- Logic Apps
tags:
  category:
  - PaaS
