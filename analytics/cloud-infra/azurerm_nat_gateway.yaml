connectors:
- Azure
name: NAT gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/natgateways' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
