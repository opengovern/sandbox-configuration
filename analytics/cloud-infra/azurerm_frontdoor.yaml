connectors:
- Azure
name: Front Door (classic)
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/frontdoors' group by 1,2;
status: inactive
tags:
  category:
  - Network
