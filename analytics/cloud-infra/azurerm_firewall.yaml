integrationType:
- azure_subscription
name: Firewall
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/azurefirewalls' group by 1,2;
status: inactive
tags:
  category:
  - Network
