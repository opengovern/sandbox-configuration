integrationType:
- azure_subscription
name: Azure Cognitive Search
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.search/searchservices' group by 1,2;
status: inactive
tags:
  category:
  - AI + ML
