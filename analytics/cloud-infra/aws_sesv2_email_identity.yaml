connectors:
- AWS
name: SES (V2) Email Identity
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::sesv2::emailidentity' group by 1,2;
status: active
tags:
  category:
  - End User
  search_terms:
  - AWS SES
  - AWS email service
  - Simple Email Service
  - AWS SES V2
  - AWS email
  - AWS email identity
  - AWS messaging
  - AWS email delivery
  - AWS email marketing
  - AWS transactional email
