connectors:
- AWS
name: Managed Airflow
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::mwaa::environment' group by 1,2;
status: active
tags:
  category:
  - Managed Services
  search_terms:
  - AWS Airflow
  - AWS managed workflow
  - AWS orchestration
  - Airflow
  - AWS data pipeline
  - AWS Apache Airflow
  - AWS workflow automation
  - AWS ETL
  - AWS task management
  - AWS data processing
