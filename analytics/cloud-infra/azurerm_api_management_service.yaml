connectors:
- Azure
name: API Management Service
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.apimanagement/service' group by 1,2;
status: inactive
tags:
  category:
  - Network
