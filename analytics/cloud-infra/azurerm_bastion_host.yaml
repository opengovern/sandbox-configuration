connectors:
- Azure
name: Azure Bastion
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.network/bastionhosts' group by 1,2;
status: inactive
tags:
  category:
  - Security
