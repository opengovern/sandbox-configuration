integrationType:
- aws_cloud_account
name: CloudTrail
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::cloudtrail::trail' group by 1,2;
status: inactive
tags:
  category:
  - Management and governance
