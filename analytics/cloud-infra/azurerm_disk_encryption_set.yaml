integrationType:
- azure_subscription
name: Disk encryption set
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.compute/diskencryptionsets' group by 1,2;
status: inactive
tags:
  category:
  - Compute
