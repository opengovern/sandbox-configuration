connectors:
- Azure
name: Network interface (NIC)
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/networkinterfaces' group by 1,2;
status: inactive
tags:
  category:
  - Network
