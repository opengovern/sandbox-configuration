integrationType:
- aws_cloud_account
name: Classic Load Balancer (Legacy)
query: select og_account_id, region, count(*) from aws_ec2_classic_load_balancer group by 1,2;
status: active
tables:
- aws::elasticloadbalancing::loadbalancer
tags:
  category:
  - Network
  search_terms:
  - Classic Load Balancer
  - AWS load balancing
  - AWS CLB
  - Load balancer
  - AWS legacy
  - AWS Elastic Load Balancing
  - CLB
  - Amazon Load Balancer
  - AWS traffic
  - AWS network
