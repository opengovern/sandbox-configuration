integrationType:
- aws_cloud_account
name: Elastic File System (EFS)
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::efs::filesystem' group by 1,2;
status: active
tags:
  category:
  - Storage
  search_terms:
  - EFS
  - AWS file storage
  - Elastic File System
  - AWS EFS
  - NFS
  - AWS NFS
  - Cloud file storage
  - AWS file system
  - Amazon EFS
  - AWS storage
