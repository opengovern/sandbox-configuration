connectors:
- Azure
name: Virtual desktop workspace
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.desktopvirtualization/workspaces' group by 1,2;
status: inactive
tags:
  category:
  - End User
