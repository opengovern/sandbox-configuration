connectors:
- Azure
name: CDN profile
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.cdn/profiles' group by 1,2;
status: inactive
tags:
  category:
  - Network
