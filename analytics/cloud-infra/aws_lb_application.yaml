connectors:
- AWS
name: Application Load Balancer
query: select og_account_id, region, count(*) from aws_ec2_application_load_balancer group by 1,2;
status: active
tables:
- aws::elasticloadbalancing::loadbalancer
tags:
  category:
  - Network
  search_terms:
  - ALB
  - AWS ALB
  - Application Load Balancer
  - AWS load balancing
  - AWS load balancer
  - AWS traffic management
  - AWS application balancing
  - AWS web traffic
  - AWS elastic load balancing
  - AWS web balancing
