integrationType:
- aws_cloud_account
name: VPN Gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::ec2::vpngateway' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
