connectors:
- AWS
name: VPN Gateway
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::ec2::vpngateway' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
