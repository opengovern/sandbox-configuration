connectors:
- Azure
name: Virtual network subnet
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.network/virtualnetworks/subnets' group by 1,2;
status: inactive
tags:
  category:
  - Network
