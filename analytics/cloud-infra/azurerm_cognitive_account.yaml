integrationType:
- azure_subscription
name: Azure Cognitive Services
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.cognitiveservices/accounts' group by 1,2;
status: active
tags:
  category:
  - AI + ML
