connectors:
- Azure
name: NetApp Account
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.netapp/netappaccounts' group by 1,2;
status: inactive
tags:
  category:
  - Storage
