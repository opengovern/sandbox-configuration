integrationType:
- azure_subscription
name: Virtual machine scale set
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.compute/virtualmachinescalesets' group by 1,2;
status: inactive
tags:
  category:
  - Compute
