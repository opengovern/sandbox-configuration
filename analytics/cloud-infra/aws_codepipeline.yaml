connectors:
- AWS
name: Code Pipeline
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::codepipeline::pipeline' group by 1,2;
status: inactive
tags:
  category:
  - DevOps
