integrationType:
- aws_cloud_account
name: CodeBuild Project
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::codebuild::project' group by 1,2;
status: inactive
tags:
  category:
  - DevOps
