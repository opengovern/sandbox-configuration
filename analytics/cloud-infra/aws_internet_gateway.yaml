integrationType:
- aws_cloud_account
name: Internet Gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::ec2::internetgateway' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
