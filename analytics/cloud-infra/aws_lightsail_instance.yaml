connectors:
- AWS
name: Lightsail Instance
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::lightsail::instance' group by 1,2;
status: active
tags:
  category:
  - PaaS
  search_terms:
  - AWS Lightsail
  - AWS VPS
  - AWS virtual server
  - AWS hosting
  - Lightsail
  - AWS cloud server
  - AWS easy deployment
  - AWS web server
  - AWS Lightsail instance
  - AWS simplified cloud
