connectors:
- AWS
name: Managed Kafka Cluster
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::kafka::cluster' group by 1,2;
status: inactive
tags:
  category:
  - Managed Services
