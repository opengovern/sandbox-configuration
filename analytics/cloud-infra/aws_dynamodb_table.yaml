integrationType:
- aws_cloud_account
name: DynamoDB Table
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::dynamodb::table' group by 1,2;
status: inactive
tags:
  category:
  - Database
