connectors:
- AWS
name: Elastic MapReduce (EMR) Cluster
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::emr::cluster' group by 1,2;
status: active
tags:
  category:
  - Data Analytics
  search_terms:
  - EMR
  - AWS EMR
  - MapReduce
  - AWS big data
  - Elastic MapReduce
  - Amazon EMR
  - Hadoop
  - AWS data processing
  - AWS Hadoop
  - Big data cluster
