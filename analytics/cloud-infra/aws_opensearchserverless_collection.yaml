connectors:
- AWS
name: OpenSearch (ElasticSearch) Serverless Collection
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::opensearchserverless::collection' group by 1,2;
status: active
tags:
  category:
  - Database
  search_terms:
  - AWS OpenSearch
  - AWS Elasticsearch
  - AWS serverless
  - AWS search service
  - AWS analytics
  - OpenSearch
  - Elasticsearch AWS
  - AWS data search
  - AWS serverless search
  - AWS managed search
