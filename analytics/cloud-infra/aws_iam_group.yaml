connectors:
- AWS
name: IAM Group
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::iam::group' group by 1,2;
status: inactive
tags:
  category:
  - Identity + Access
