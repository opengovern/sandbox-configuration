integrationType:
- aws_cloud_account
name: S3 Glacier Vault
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::glacier::vault' group by 1,2;
status: active
tags:
  category:
  - Storage
  search_terms:
  - S3 Glacier
  - AWS Glacier
  - Cold storage
  - AWS archive
  - Amazon Glacier
  - AWS data archive
  - Glacier Vault
  - AWS long-term storage
  - AWS S3
  - AWS low-cost storage
