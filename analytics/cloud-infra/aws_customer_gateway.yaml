connectors:
- AWS
name: Customer Gateway
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'aws::ec2::customergateway' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
