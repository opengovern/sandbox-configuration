connectors:
- Azure
name: Private endpoint
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/privateendpoints' group by 1,2;
status: inactive
tags:
  category:
  - Network
