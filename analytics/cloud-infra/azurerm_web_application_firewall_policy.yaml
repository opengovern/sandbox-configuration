connectors:
- Azure
name: Web Application Firewall (WAF) policy
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.network/firewallpolicies' group by 1,2;
status: inactive
tags:
  category:
  - Security
