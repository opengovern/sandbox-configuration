connectors:
- Azure
name: Virtual network
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/virtualnetworks' group by 1,2;
status: active
tags:
  category:
  - Network
  search_terms:
  - Azure VNet
  - Azure network
  - Azure networking
  - Azure virtual network
  - Microsoft Azure network
  - Azure cloud network
  - Azure VPN
  - Azure private network
  - Azure network service
  - Azure VPC
