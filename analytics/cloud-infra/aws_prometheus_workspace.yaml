connectors:
- AWS
name: Managed Prometheus Workspace
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::amp::workspace' group by 1,2;
status: inactive
tags:
  category:
  - Managed Services
