connectors:
- AWS
name: ElastiCache Cluster
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::elasticache::cluster' group by 1,2;
status: active
tags:
  category:
  - Database
  search_terms:
  - ElastiCache
  - AWS cache
  - Redis
  - Memcached
  - AWS Redis
  - AWS Memcached
  - Cache cluster
  - AWS in-memory
  - ElastiCache cluster
  - AWS caching
