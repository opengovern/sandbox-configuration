connectors:
- AWS
name: Local Gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::ec2::localgateway' group by 1,2;
status: active
tags:
  category:
  - Network Gateways
  search_terms:
  - AWS Local Gateway
  - AWS on-premises connection
  - AWS gateway
  - AWS network gateway
  - AWS hybrid cloud
  - AWS local network
  - AWS direct connect
  - AWS cloud integration
  - AWS local connectivity
  - AWS network interface
