integrationType:
- aws_cloud_account
name: CloudFront (CDN) Distribution
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::cloudfront::distribution' group by 1,2;
status: inactive
tags:
  category:
  - Network
