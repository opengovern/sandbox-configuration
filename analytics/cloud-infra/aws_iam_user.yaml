connectors:
- AWS
name: IAM User
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::iam::user' group by 1,2;
status: inactive
tags:
  category:
  - Identity + Access
