connectors:
- AWS
name: Network Load Balancer
query: select og_account_id, region, count(*) from aws_ec2_network_load_balancer group by 1,2;
status: active
tables:
- aws::elasticloadbalancing::loadbalancer
tags:
  category:
  - Network
  search_terms:
  - NLB
  - AWS NLB
  - Network Load Balancer
  - AWS network balancing
  - AWS TCP load balancer
  - AWS layer 4 load balancer
  - AWS load balancing
  - AWS network traffic
  - AWS high-performance load balancer
  - AWS elastic load balancing
