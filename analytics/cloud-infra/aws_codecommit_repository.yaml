connectors:
- AWS
name: Code Commit Repository
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::codecommit::repository' group by 1,2;
status: inactive
tags:
  category:
  - DevOps
