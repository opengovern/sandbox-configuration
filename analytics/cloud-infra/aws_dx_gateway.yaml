connectors:
- AWS
name: Direct Connect Gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::directconnect::gateway' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
