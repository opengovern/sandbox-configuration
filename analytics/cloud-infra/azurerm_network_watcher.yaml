connectors:
- Azure
name: Network Watcher
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/networkwatchers' group by 1,2;
status: inactive
tags:
  category:
  - Network
