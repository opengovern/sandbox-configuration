connectors:
- Azure
name: Container registry
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.containerregistry/registries' group by 1,2;
status: inactive
tags:
  category:
  - Containers
