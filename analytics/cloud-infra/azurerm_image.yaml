integrationType:
- azure_subscription
name: VM Image template
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.virtualmachineimages/imagetemplates' group by 1,2;
status: inactive
tags:
  category:
  - Compute
