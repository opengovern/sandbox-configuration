connectors:
- Azure
name: Automation account
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.automation/automationaccounts' group by 1,2;
status: inactive
tags:
  category:
  - Management and governance
