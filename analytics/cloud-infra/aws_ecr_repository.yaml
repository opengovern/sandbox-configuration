connectors:
- AWS
name: Container (ECR) Repository
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::ecr::repository' group by 1,2;
status: inactive
tags:
  category:
  - Containers
