connectors:
- Azure
name: Traffic Manager profile
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.network/trafficmanagerprofiles' group by 1,2;
status: inactive
tags:
  category:
  - Network
