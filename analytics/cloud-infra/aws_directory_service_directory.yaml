connectors:
- AWS
name: Directory Service - Identity Directory
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::directoryservice::directory' group by 1,2;
status: inactive
tags:
  category:
  - Identity + Access
