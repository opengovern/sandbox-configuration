connectors:
- AWS
name: Cassandra Keyspace
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::keyspaces::keyspace' group by 1,2;
status: inactive
tags:
  category:
  - Database
