integrationType:
- azure_subscription
name: Virtual network gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/virtualnetworkgateways' group by 1,2;
status: inactive
tags:
  category:
  - Network Gateways
