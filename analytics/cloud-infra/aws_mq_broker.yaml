connectors:
- AWS
name: AWS MQ Broker
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::mq::broker' group by 1,2;
status: inactive
tags:
  category:
  - Messaging
