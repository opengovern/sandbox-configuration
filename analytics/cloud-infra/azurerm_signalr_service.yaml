connectors:
- Azure
name: SignalR
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.signalrservice/signalr' group by 1,2;
status: inactive
tags:
  category:
  - DevOps
