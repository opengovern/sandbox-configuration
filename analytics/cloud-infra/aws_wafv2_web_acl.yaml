integrationType:
- aws_cloud_account
name: Web Firewall (WAF v2)
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::wafv2::webacl' group by 1,2;
status: inactive
tags:
  category:
  - Network Security
