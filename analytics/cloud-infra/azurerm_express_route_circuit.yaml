connectors:
- Azure
name: ExpressRoute circuit
query: select connection_id, region, count(*) from kaytu_lookup where resource_type = 'microsoft.network/expressroutecircuits' group by 1,2;
status: inactive
tags:
  category:
  - Network
