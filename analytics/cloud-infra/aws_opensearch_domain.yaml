connectors:
- AWS
name: OpenSearch (ElasticSearch) Domains
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::opensearch::domain' group by 1,2;
status: active
tags:
  category:
  - Database
  search_terms:
  - OpenSearch
  - AWS OpenSearch
  - ElasticSearch
  - AWS search
  - AWS ElasticSearch
  - Search domain
  - AWS data search
  - OpenSearch service
  - AWS search engine
  - Amazon OpenSearch
