connectors:
- AWS
name: Elastic Container Service (ECS) Cluster
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::ecs::cluster' group by 1,2;
status: inactive
tags:
  category:
  - Containers
