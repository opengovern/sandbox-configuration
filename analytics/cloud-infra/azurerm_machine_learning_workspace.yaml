connectors:
- Azure
name: Azure Machine Learning workspace
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.machinelearningservices/workspaces' group by 1,2;
status: inactive
tags:
  category:
  - AI + ML
