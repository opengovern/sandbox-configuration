integrationType:
- azure_subscription
name: AKS (Kubernetes) cluster
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.containerservice/managedclusters' group by 1,2;
status: active
tags:
  category:
  - Containers
  search_terms:
  - AKS
  - Azure Kubernetes
  - Kubernetes
  - Azure container
  - AKS cluster
  - Azure cloud
  - AKS deployment
  - Azure orchestration
  - Microsoft AKS
  - Azure Kubernetes Service
