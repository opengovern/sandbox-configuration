connectors:
- Azure
name: Route table
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.network/routetables' group by 1,2;
status: inactive
tags:
  category:
  - Network
