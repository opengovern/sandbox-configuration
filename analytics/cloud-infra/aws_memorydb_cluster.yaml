integrationType:
- aws_cloud_account
name: MemoryDB Cluster
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::memorydb::cluster' group by 1,2;
status: active
tags:
  category:
  - Database
  search_terms:
  - MemoryDB
  - AWS MemoryDB
  - In-memory database
  - Redis compatible
  - AWS Redis
  - AWS database
  - MemoryDB cluster
  - AWS fast database
  - Amazon MemoryDB
  - AWS caching
