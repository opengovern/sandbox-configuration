connectors:
- AWS
name: Redshift Cluster
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::redshift::cluster' group by 1,2;
status: active
tags:
  category:
  - Database
  search_terms:
  - AWS Redshift
  - Redshift
  - AWS data warehouse
  - AWS cloud warehouse
  - AWS big data
  - Redshift database
  - AWS Redshift service
  - AWS analytics
  - AWS SQL warehouse
  - AWS scalable warehouse
