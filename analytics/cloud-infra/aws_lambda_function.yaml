integrationType:
- aws_cloud_account
name: Lambda Function
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::lambda::function' group by 1,2;
status: active
tags:
  category:
  - PaaS
  search_terms:
  - Lambda
  - AWS Lambda
  - Serverless
  - AWS serverless
  - Lambda functions
  - AWS functions
  - AWS compute
  - Function-as-a-Service
  - AWS FaaS
  - Lambda computing
