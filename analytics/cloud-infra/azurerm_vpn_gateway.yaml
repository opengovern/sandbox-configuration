connectors:
- Azure
name: VPN Gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/vpngateways' group by 1,2;
status: inactive
tags:
  category:
  - Security
