integrationType:
- azure_subscription
name: Application gateway
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.network/applicationgateways' group by 1,2;
status: active
tags:
  category:
  - Network
  search_terms:
  - Application Gateway
  - Azure Gateway
  - Azure WAF
  - Azure web application firewall
  - Azure application proxy
  - Azure load balancing
  - Azure routing
  - Azure web traffic
  - Azure application security
  - Azure front-end
