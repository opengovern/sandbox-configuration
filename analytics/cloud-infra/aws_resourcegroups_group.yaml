connectors:
- AWS
name: Resource Groups
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::resourcegroups::groups' group by 1,2;
status: inactive
tags:
  category:
  - Governance
