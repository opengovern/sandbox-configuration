connectors:
- AWS
name: VPC (Virtual Network)
query: select connection_id, region, count(*) from og_lookup where resource_type = 'aws::ec2::vpc' group by 1,2;
status: active
tags:
  category:
  - Network
  search_terms:
  - VPC
  - AWS VPC
  - Amazon VPC
  - AWS network
  - AWS virtual network
  - Amazon virtual private cloud
  - AWS cloud network
  - Virtual Private Cloud
  - AWS private network
  - AWS networking
