connectors:
- AWS
name: EBS Volume (Disk)
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'aws::ec2::volume' group by 1,2;
status: active
tags:
  category:
  - Storage
  search_terms:
  - EBS
  - AWS EBS
  - Amazon EBS
  - Elastic Block Store
  - AWS storage
  - AWS disk
  - AWS block storage
  - Amazon block storage
  - EBS volume
  - AWS volume
