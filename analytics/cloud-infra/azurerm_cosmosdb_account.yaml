integrationType:
- azure_subscription
name: Azure Cosmos DB Account
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.documentdb/databaseaccounts' group by 1,2;
status: inactive
tags:
  category:
  - Database
