connectors:
- Azure
name: Application Insights
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.applicationinsights/components' group by 1,2;
status: inactive
tags:
  category:
  - Management and governance
