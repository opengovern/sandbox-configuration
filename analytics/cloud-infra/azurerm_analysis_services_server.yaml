connectors:
- Azure
name: Azure Analysis Services server
query: select connection_id, region, count(*) from platform_lookup where resource_type = 'microsoft.analysisservices/servers' group by 1,2;
status: inactive
tags:
  category:
  - Data Analytics
