connectors:
- Azure
name: Event Grid domain
query: select connection_id, region, count(*) from og_lookup where resource_type = 'microsoft.eventgrid/domains' group by 1,2;
status: inactive
tags:
  category:
  - Data Analytics
