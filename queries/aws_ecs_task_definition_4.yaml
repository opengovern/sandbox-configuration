Description: Allows users to query AWS ECS Task Definitions to gain insights into
  the configuration of running tasks in an ECS service. The table provides details
  such as task definition ARN, family, network mode, revision, status, and more.
ID: aws_ecs_task_definition_4
IntegrationType:
- aws_cloud_account
Query:
  Engine: CloudQL-v0.0.1
  ListOfTables: []
  Parameters: []
  PrimaryTable: null
  QueryToExecute: "select\n  task_definition_arn,\n  cd ->> 'Name' as container_name,\n\
    \  cd ->> 'LogConfiguration' as log_configuration\nfrom\n  aws_ecs_task_definition,\n\
    \  jsonb_array_elements(container_definitions) as cd\nwhere\n cd ->> 'LogConfiguration'\
    \ is null;"
Tags:
  cloud_asset_management:
  - 'true'
  cloud_ops:
  - 'true'
  cloud_provider:
  - aws
  cloud_service:
  - ECS
Title: Find AWS ECS Task Definitions Without Log Config
