Description: Allows users to query AWS EC2 Transit Gateway resources for detailed
  information on configuration, status, and associations.
ID: aws_ec2_transit_gateway_4
IntegrationTypeName:
- aws_cloud
Query:
  Engine: CloudQL-v0.0.1
  ListOfTables: []
  Parameters: []
  PrimaryTable: null
  QueryToExecute: "select\n  transit_gateway_id,\n  tags\nfrom\n  aws_ec2_transit_gateway\n\
    where\n  not tags :: JSONB ? 'application';"
Tags:
  cloud_asset_management:
  - 'true'
  cloud_provider:
  - aws
  cloud_service:
  - EC2 Transit Gateway
Title: Find AWS EC2 Transit Gateway Details
