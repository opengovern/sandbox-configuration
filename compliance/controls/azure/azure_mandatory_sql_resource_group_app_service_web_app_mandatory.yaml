ID: azure_mandatory_sql_resource_group_app_service_web_app_mandatory
Title: "App Service web apps should have mandatory tags"
Description: "Check if App Service web apps have mandatory tags."
Query:
  Engine: steampipe-v0.5
  QueryToExecute: |
    with analysis as (
      select
        og_account_id,
        og_resource_id,
        id,
        title,
        tags ?& '{{.azureMandatoryTags}}'::text[] as has_mandatory_tags,
        to_jsonb('{{.azureMandatoryTags}}'::text[]) - array(select jsonb_object_keys(tags)) as missing_tags,
        resource_group, subscription_id
      from
        azure_app_service_web_app
    )
    select
        og_account_id,
        og_resource_id,
        id as resource,
      case
        when has_mandatory_tags then 'ok'
        else 'alarm'
      end as status,
      case
        when has_mandatory_tags then title || ' has all mandatory tags.'
        else title || ' is missing tags: ' || array_to_string(array(select jsonb_array_elements_text(missing_tags)), ', ') || '.'
      end as reason,
      resource_group, subscription_id
    from
      analysis;
  PrimaryTable: azure_app_service_web_app
  ListOfTables:
    - azure_app_service_web_app
  Parameters:
    - key: azureMandatoryTags
      required: true
Severity: high
Tags: {}
Integration_Type_Name:
  - azure_subscription
