ID: aws_cis_v300_2_4_1
Title: 2.4.1 Ensure that encryption is enabled for EFS file systems
Description: EFS data should be encrypted at rest using AWS KMS (Key Management Service).
Query:
  Engine: odysseus-v0.0.1
  QueryToExecute: "    select\n      arn as resource,\n      case\n        when encrypted
    then 'ok'\n        else 'alarm'\n      end as status,\n      case\n        when
    encrypted then title || ' encrypted at rest.'\n        else title || ' not encrypted
    at rest.'\n      end as reason\n      \n      \n    from\n      aws_efs_file_system;\n"
  Connector:
  - aws
  PrimaryTable: ""
  ListOfTables: []
  Parameters: []
  Global: false
Tags: {}
Severity: ""
